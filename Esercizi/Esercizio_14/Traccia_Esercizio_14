Implementare un programma parallelo per l’ambiente multicore con npxnp unità processanti che impieghi la libreria OpenMP. 
Il programma deve essere organizzato come segue: il core master deve leggere una matrice A di dimensione MxM. 
Quindi, ogni core deve estrarre un blocco della matrice A di dimensione (M/np) x (M/np), 
conservando i valori in npxnp matrici. Infine, i core devono collaborare per calcolare la somma di tali matrici.
